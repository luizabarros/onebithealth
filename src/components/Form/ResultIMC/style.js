import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    imcResult: {
        fontSize: 20,
        textAlign: 'center',
        fontWeight: '400',
        marginTop: 5,
    },
    
    imcMessage: {
        fontSize: 25,
        fontWeight: '600',
        marginTop: 50
    },

    shareIMC: {
        marginTop: 20,
        fontWeight: '600',
        fontSize: 15,
        textAlign: 'center',
    }
});

export default styles;